<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.tileverse.parquetry</groupId>
  <artifactId>parquetry-parent</artifactId>
  <version>${revision}</version>
  <packaging>pom</packaging>

  <name>Parquetry :: Parent</name>
  <description>Hadoop-free Java 25 Parquet reader+writer.</description>
  <url>https://tileverse.io</url>

  <organization>
    <name>Tileverse.io</name>
    <url>https://tileverse.io</url>
  </organization>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <name>Gabriel Roldan</name>
      <email>groldan@multivers.io</email>
      <organization>Multiversio LLC</organization>
      <organizationUrl>https://multivers.io</organizationUrl>
    </developer>
  </developers>

  <modules>
    <module>dependencies</module>
    <module>bom</module>
    <module>parquetry-io</module>
    <module>parquetry-compression</module>
    <module>parquetry-format</module>
    <module>parquetry-core</module>
    <module>parquetry-catalog</module>
    <module>parquetry-encryption</module>
    <module>integrations</module>
    <module>internal</module>
    <module>apps</module>
  </modules>

  <scm>
    <connection>scm:git:https://github.com/tileverse-io/parquetry.git</connection>
    <developerConnection>scm:git:git@github.com:tileverse-io/parquetry.git</developerConnection>
    <tag>HEAD</tag>
    <url>https://github.com/tileverse-io/parquetry</url>
  </scm>

  <issueManagement>
    <system>GitHub Issues</system>
    <url>https://github.com/tileverse-io/parquetry/issues</url>
  </issueManagement>

  <distributionManagement>
    <repository>
      <id>central</id>
      <url>https://central.sonatype.com/api/v1/publisher/deployments/upload</url>
    </repository>
    <snapshotRepository>
      <id>central</id>
      <url>https://central.sonatype.com/repository/maven-snapshots/</url>
    </snapshotRepository>
  </distributionManagement>

  <properties>
    <revision>1.0-SNAPSHOT</revision>

    <!-- GPG signing is off by default; the release pipeline flips it to false. -->
    <gpg.skip>true</gpg.skip>
    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.target>25</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <tileverse-storage.version>2.0-SNAPSHOT</tileverse-storage.version>
    <jackson.version>3.1.2</jackson.version>
    <opentelemetry.version>1.46.0</opentelemetry.version>
    <aircompressor.version>3.6</aircompressor.version>
    <brotli-dec.version>0.1.2</brotli-dec.version>
    <commons-compress.version>1.27.1</commons-compress.version>
    <commons-codec.version>1.18.0</commons-codec.version>
    <xz.version>1.10</xz.version>
    <slf4j.version>2.0.17</slf4j.version>
    <error-prone-annotations.version>2.43.0</error-prone-annotations.version>
    <jspecify.version>1.0.0</jspecify.version>
    <lombok.version>1.18.46</lombok.version>

    <junit.version>5.11.3</junit.version>
    <assertj.version>3.26.3</assertj.version>
    <testcontainers.version>2.0.5</testcontainers.version>
    <jts.version>1.20.0</jts.version>
    <jsqlparser.version>5.3</jsqlparser.version>
    <arrow.version>19.0.0</arrow.version>
    <duckdb.version>1.5.3.0</duckdb.version>
    <flatbuffers.version>25.2.10</flatbuffers.version>
    <parquet-java.version>1.17.0</parquet-java.version>
    <picocli.version>4.7.6</picocli.version>
    <cucumber.version>7.20.1</cucumber.version>
    <native-maven-plugin.version>0.10.6</native-maven-plugin.version>

    <spotless.version>3.5.1</spotless.version>
    <sortpom.version>4.0.0</sortpom.version>
    <jacoco.version>0.8.14</jacoco.version>

    <!-- JaCoCo argLine property (overridden by jacoco:prepare-agent when coverage profile is active) -->
    <argLine/>
    <!-- Append-only JVM args for the test forks. Set on the CLI to pass ad-hoc options without clobbering the
         jacoco-owned @{argLine} or the preview flags, e.g. -DextraArgLine="-Xmx2g". Empty by default. -->
    <extraArgLine/>
  </properties>

  <repositories>
    <repository>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
      <id>central-portal-snapshots</id>
      <name>Central Portal Snapshots</name>
      <url>https://central.sonatype.com/repository/maven-snapshots/</url>
    </repository>
  </repositories>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.2</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.15.0</version>
          <configuration>
            <release>25</release>
            <parameters>true</parameters>
            <compilerArgs>
              <arg>--enable-preview</arg>
            </compilerArgs>
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>3.5.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
          <configuration>
            <!-- @{argLine} lets jacoco:prepare-agent inject its agent when the coverage profile is active;
                 ${extraArgLine} appends ad-hoc CLI JVM args (e.g. -DextraArgLine="-Xmx2g") to the fork. -->
            <argLine>@{argLine} --enable-preview --enable-native-access=ALL-UNNAMED ${extraArgLine}</argLine>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>3.5.2</version>
          <configuration>
            <!-- @{argLine} lets jacoco:prepare-agent-integration inject its agent when the coverage profile is active;
                 ${extraArgLine} appends ad-hoc CLI JVM args (e.g. -DextraArgLine="-Xmx2g") to the fork. -->
            <argLine>@{argLine} --enable-preview --enable-native-access=ALL-UNNAMED ${extraArgLine}</argLine>
            <!-- Aggregator modules (parent, BOMs, coverage-report) have no ITs; skip the IT phase there. -->
            <failIfNoTests>false</failIfNoTests>
            <failIfNoSpecifiedTests>false</failIfNoSpecifiedTests>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco.version}</version>
          <executions>
            <execution>
              <id>prepare-agent</id>
              <goals>
                <goal>prepare-agent</goal>
              </goals>
            </execution>
            <execution>
              <id>prepare-agent-integration</id>
              <goals>
                <goal>prepare-agent-integration</goal>
              </goals>
            </execution>
            <execution>
              <id>report</id>
              <goals>
                <goal>report</goal>
              </goals>
              <phase>test</phase>
            </execution>
            <execution>
              <id>report-integration</id>
              <goals>
                <goal>report-integration</goal>
              </goals>
              <phase>post-integration-test</phase>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <version>${spotless.version}</version>
          <configuration>
            <java>
              <palantirJavaFormat>
                <version>2.90.0</version>
                <formatJavadoc>true</formatJavadoc>
              </palantirJavaFormat>
              <licenseHeader>
                <content>/*
 * Copyright (c) 2026 Multivers.io
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */</content>
              </licenseHeader>
              <removeUnusedImports/>
              <importOrder>
                <order>java,javax,org,com,io.tileverse.storage,io.tileverse.parquetry,</order>
              </importOrder>
            </java>
            <pom>
              <sortPom>
                <expandEmptyElements>false</expandEmptyElements>
              </sortPom>
            </pom>
          </configuration>
          <executions>
            <execution>
              <goals>
                <goal>apply</goal>
              </goals>
              <phase>validate</phase>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>flatten-maven-plugin</artifactId>
          <version>1.6.0</version>
          <executions>
            <execution>
              <id>flatten</id>
              <goals>
                <goal>flatten</goal>
              </goals>
              <phase>process-resources</phase>
              <configuration>
                <updatePomFile>true</updatePomFile>
                <flattenMode>resolveCiFriendliesOnly</flattenMode>
              </configuration>
            </execution>
            <execution>
              <id>flatten.clean</id>
              <goals>
                <goal>clean</goal>
              </goals>
              <phase>clean</phase>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>3.3.1</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>3.11.2</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-gpg-plugin</artifactId>
          <version>3.2.8</version>
        </plugin>
        <plugin>
          <groupId>org.sonatype.central</groupId>
          <artifactId>central-publishing-maven-plugin</artifactId>
          <version>0.10.0</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <configuration>
          <!-- Tolerate missing javadoc on builders and internal classes; report real javadoc errors. -->
          <doclint>all,-missing</doclint>
          <!-- BOM modules have no real javadoc links to resolve; skip auto-detection to keep the build offline-friendly. -->
          <detectLinks>false</detectLinks>
          <detectOfflineLinks>false</detectOfflineLinks>
          <!-- parquetry compiles against Java 25 with preview features (see maven-compiler-plugin). -->
          <release>25</release>
          <additionalJOptions>
            <additionalJOption>--enable-preview</additionalJOption>
          </additionalJOptions>
        </configuration>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <!-- Central Portal Publishing -->
        <groupId>org.sonatype.central</groupId>
        <artifactId>central-publishing-maven-plugin</artifactId>
        <extensions>true</extensions>
        <configuration>
          <publishingServerId>central</publishingServerId>
          <deploymentName>Parquetry</deploymentName>
          <autoPublish>true</autoPublish>
          <waitUntil>published</waitUntil>
          <waitMaxTime>3600</waitMaxTime>
          <excludeArtifacts>
            <excludeArtifact>parquetry-internal</excludeArtifact>
            <excludeArtifact>parquetry-testkit</excludeArtifact>
            <excludeArtifact>parquetry-benchmarks</excludeArtifact>
            <excludeArtifact>parquetry-probes</excludeArtifact>
            <excludeArtifact>parquetry-coverage-report</excludeArtifact>
            <excludeArtifact>apps</excludeArtifact>
            <excludeArtifact>par</excludeArtifact>
          </excludeArtifacts>
        </configuration>
      </plugin>
      <plugin>
        <!-- GPG Signing (only when publishing) -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-gpg-plugin</artifactId>
        <executions>
          <execution>
            <id>sign-artifacts</id>
            <goals>
              <goal>sign</goal>
            </goals>
            <phase>verify</phase>
            <configuration>
              <skip>${gpg.skip}</skip>
              <gpgArguments>
                <arg>--pinentry-mode</arg>
                <arg>loopback</arg>
              </gpgArguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <!--
        Satisfies the enforcer dependencyConvergence rule: no transitive dependency on the non-test classpath
        may resolve two different versions for the same GAV. Overrides go in this profile's dependencyManagement
        so they're only paid when convergence is actually being checked. Active by default; opt out with
        -DskipDependencyConvergence.
      -->
      <id>dependencyConvergence</id>
      <activation>
        <activeByDefault>false</activeByDefault>
        <property>
          <name>!skipDependencyConvergence</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
            <executions>
              <execution>
                <id>enforce-dependency-convergence</id>
                <goals>
                  <goal>enforce</goal>
                </goals>
                <configuration>
                  <rules>
                    <dependencyConvergence>
                      <excludedScopes>test</excludedScopes>
                    </dependencyConvergence>
                  </rules>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>qa</id>
      <build>
        <plugins>
          <plugin>
            <groupId>com.diffplug.spotless</groupId>
            <artifactId>spotless-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>check</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>validate</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>coverage</id>
      <activation>
        <property>
          <name>coverage</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
